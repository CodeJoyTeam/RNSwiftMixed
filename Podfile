source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '9.0'
use_frameworks!

target 'RNSwiftMixed' do

pod 'React', :path => './node_modules/react-native', :subspecs => [#pod的路径
    'Core',
    'RCTText',
    'RCTNetwork',
    'RCTWebSocket', # 你需要用到的库
  ]
end
